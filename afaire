- rapatrier les erreurs de builtin au lieu de lancer a execve() pour celles 
qui n'ont pas de binaires : env, unset, exit, export, cd (vs : echo et pwd) 
---> only unset remaining

- SEGFAULT if cwd is suppressed. ie: mkdir -p a/b && cd a/b && rm -r ../../a ; cd .. 
	- > check for directory existence before chdir? not enough : 
	--> the problem is not related to cd. any cmd after rm the cwd leads to SIGSEV in:
		- check_error (main_ut_debug line 49), which leads to
		- check_syntax line 38 while accessing g_checkers[type](current) with type == "ambigous_word". 
		j'arrive pas a analyser davantage. j'imagine qu'on accede a un mauvais index de g_checkers?

- export "" -> minishell err is '""' : not a valid id. but why is there still "", quote removal must lead to an empty stuff ??
	is this linked to the correction of echo "" truc ? 
- exportHOLA=bonjour -> does not cmd not found. code 0 but var is not set to env. tested with other builtin, cmd not found is raised. 
- export cmd="ls -la". cmd -> cmd not found "ls -la". expansion to several words must be splitted into several word?
- after un "unset PATH", un "cat Makefile" -> No such file or dir (vs cmd not found). code 127 ok.

- test 16 des redirections qui tourne dans le vide

- pb heredoc sans commande derriere : unlink: no such file or directory

- buitlin (pwd and echo only) to be called with execve not found. modify cmd fullname, cmdname et cmd_args[0] to do

- expansion / quote removal autour d'un argument : il ne faut pas supp les whitespaces
  echo "    " -> doit output les whitespaces (cf. test echo 14)
- expansion de $EMPTY vs "$EMPTY" :
  -> $EMPTY : code retour 0
  -> "$EMPTY" : code 127 + cmd not found

- here_doc -> mauvais code erreur + erreurs si CTRL-D ou CTRL-C
